# Borrow Buddy: Tasks

อ้างอิง [CONTEXT.md](./CONTEXT.md) และ [design.md](./design.md) ทำตามลำดับ ติ๊กเมื่อเสร็จ

กติกาการทำงาน: JavaScript เท่านั้น, ห้ามลบไฟล์โดยไม่ถามก่อน, ตอนตั้งค่า Vite/React/Vitest ให้ตรวจเอกสารล่าสุดผ่าน Context7 ก่อน

## เฟส 1: ตั้งโปรเจ็กต์

- [x] T1.1 สร้างโปรเจ็กต์ Vite + React (เทมเพลต JavaScript) ในโฟลเดอร์ `borrow-buddy` โดยไม่ทับ `CONTEXT.md` และ `design.md`
- [x] T1.2 ติดตั้ง Vitest และเพิ่มสคริปต์ `test` ใน `package.json`
- [x] T1.3 สร้างโครงโฟลเดอร์ `src/components` และ `src/lib` ตาม design.md ข้อ 7

- **เสร็จเมื่อ:** `npm run dev` เปิดหน้าเปล่าได้ และ `npm test` รันได้

## เฟส 2: ตรรกะ (`src/lib`) ทำแบบเขียนเทสต์ก่อน

- [x] T2.1 `dateFormat.js`: แปลง `YYYY-MM-DD` เป็น พ.ศ. แบบ "24 ก.ย. 2569" + เทสต์
- [x] T2.2 `loanRules.js`: คำนวณสถานะ (ยังไม่คืน / เกินกำหนด / คืนแล้ว) รับ "วันนี้" เป็นพารามิเตอร์ + เทสต์ขอบ (วันนี้ = กำหนดคืนต้องยังไม่เกิน, วันถัดไปเกิน)
- [x] T2.3 `loanRules.js`: จำนวนวันเกินกำหนด + เทสต์
- [x] T2.4 `loanRules.js`: ตรวจความถูกต้องของ Loan (ชื่อเพื่อน/ของห้ามว่าง, กำหนดคืนและวันที่คืนจริงต้องไม่ก่อนวันที่ยืม) คืนข้อความผิดพลาดภาษาไทย + เทสต์ทุกกรณีผิด
- [x] T2.5 `loanRules.js`: จัดกลุ่ม เกินกำหนด → ยังไม่คืน → คืนแล้ว และเรียงตามกำหนดคืนใกล้สุดในแต่ละกลุ่ม + เทสต์
- [x] T2.6 `loanRules.js`: กรองตามชื่อเพื่อน + เทสต์
- [x] T2.7 `loanRules.js`: กดคืนแล้ว (ค่าเริ่มต้นวันนี้ แก้ย้อนหลังได้) และยกเลิกการคืน + เทสต์
- [x] T2.8 `storage.js`: อ่าน/เขียน Loan ใน localStorage คีย์เดียว, JSON เสียหรืออ่านไม่ได้ให้คืนรายการว่างพร้อมสัญญาณแจ้งเตือน ไม่เขียนทับข้อมูลเดิม

- **เสร็จเมื่อ:** `npm test` ผ่านทั้งหมด

## เฟส 3: UI

- [x] T3.1 `App.jsx`: เก็บ state Loan โหลดจาก storage ตอนเปิด บันทึกทุกครั้งที่เปลี่ยน และแสดงข้อความภาษาไทยเมื่ออ่านข้อมูลไม่ได้
- [x] T3.2 `LoanForm.jsx`: ฟอร์มเดียวใช้เพิ่มและแก้ไข (เพื่อน, ของ, วันที่ยืม ค่าเริ่มต้นวันนี้, กำหนดคืน) แสดงข้อผิดพลาดจากการตรวจ มีปุ่มยกเลิกการแก้ไข
- [x] T3.3 `SearchBox.jsx`: ช่องค้นหาตามชื่อเพื่อน
- [x] T3.4 `LoanList.jsx` และ `LoanItem.jsx`: แสดงกลุ่มและรายการตาม design.md ข้อ 6 (วันที่ พ.ศ., จำนวนวันเกินเฉพาะเกินกำหนด, วันที่คืนจริงเฉพาะคืนแล้ว)
- [x] T3.5 ปุ่มต่อรายการ: คืนแล้ว (เลือกวันที่ได้), ยกเลิกการคืน, แก้ไข ไม่มีปุ่มลบ
- [x] T3.6 `ThemeToggle.jsx`: โหมดมืด สลับได้และจำค่าไว้
- [x] T3.7 สไตล์: รองรับมือถือ, สถานะแยกด้วยข้อความและสี (ไม่พึ่งสีอย่างเดียว)

- **เสร็จเมื่อ:** ใช้งานครบทุกอย่างในขอบเขตผ่านหน้าเว็บได้

## เฟส 4: ตรวจรับ

- [x] T4.1 รัน `npm test` ผ่านทั้งหมด
- [x] T4.2 ทดลองในเบราว์เซอร์: เพิ่ม → แก้ไข → กดคืน → ยกเลิกคืน → รีเฟรชแล้วข้อมูลยังอยู่
- [x] T4.3 ทดลองกรณีขอบ: กำหนดคืนวันนี้, กำหนดคืนเมื่อวาน, กรอกวันที่ผิดกติกา, ข้อมูลใน localStorage เสีย
- [x] T4.4 ตรวจหน้าจอมือถือและโหมดมืด
- [x] T4.5 ตรวจว่าไม่มีฟีเจอร์นอกขอบเขต (ลบ, แจ้งเตือน, เลื่อนกำหนด, รูปภาพ, สำรองข้อมูล) และศัพท์ในหน้าเว็บตรงกับ CONTEXT.md

---

# เวอร์ชัน 2: Supabase + เข้าสู่ระบบ

อ้างอิง design.md ข้อ 0, 4, 6, 8, 9, 10 กติกาเพิ่ม: ตรวจเอกสาร Supabase ล่าสุด (Context7 หรือ Supabase MCP `search_docs`) ก่อนเขียนโค้ดที่เกี่ยวข้อง, ห้าม commit ค่า key จริง, ห้ามใช้ service role key ฝั่งเว็บ

## เฟส 5: ตั้งค่า Supabase

- [x] T5.1 ตรวจตารางเดิมในโปรเจ็กต์ Supabase ก่อนเปลี่ยน schema
- [x] T5.2 เขียน migration ใน `supabase/migrations/`: ตาราง `loans` ตาม design.md ข้อ 4 (คอลัมน์, check constraint, index `owner_id`, trigger `updated_at`)
- [x] T5.3 เปิด RLS และสร้าง policy `SELECT`/`INSERT`/`UPDATE` สำหรับ `authenticated` ตาม design.md ข้อ 6 ไม่มี policy `DELETE`
- [ ] T5.4 ปิดการสมัครสมาชิกเองใน Dashboard และสร้างบัญชีเจ้าของ (Add user, ยืนยัน email แล้ว)
- [x] T5.5 รัน Security Advisor แก้จนไม่มีคำเตือนเรื่อง RLS/ความปลอดภัย
- [x] T5.6 ติดตั้ง `@supabase/supabase-js`, สร้าง `.env.example` และ `.env.local` (อยู่ใน `.gitignore`)

- **เสร็จเมื่อ:** ตาราง `loans` มี RLS, signup ปิด, มีบัญชีเจ้าของ 1 บัญชี, Advisor ไม่มีคำเตือน

## เฟส 6: ตรรกะและชั้นข้อมูล (`src/lib`) เขียนเทสต์ก่อน

- [x] T6.1 `loanMapper.js`: แปลง Loan ↔ แถว `loans` ไป-กลับ, ไม่ส่ง `id`/`owner_id` ตอน insert + เทสต์
- [x] T6.2 `authErrors.js`: แปลงข้อผิดพลาด Supabase เป็นข้อความไทย (ไม่บอกว่า email หรือรหัสผ่านผิด) + เทสต์
- [x] T6.3 `supabaseClient.js`: สร้าง client จาก `VITE_SUPABASE_URL` / `VITE_SUPABASE_PUBLISHABLE_KEY` แจ้งข้อความชัดเจนถ้าไม่ได้ตั้งค่า
- [x] T6.4 `auth.js`: เข้าสู่ระบบ, ออกจากระบบ, ติดตาม session (รับ client เป็นพารามิเตอร์ได้)
- [x] T6.5 `loanRepo.js`: list / create / update ผ่าน Supabase ไม่มี delete + เทสต์ด้วย client ปลอม
- [x] T6.6 `legacyStorage.js` (ปรับจาก `storage.js`): อ่าน Loan เดิม, ธงนำเข้าแล้ว, แยกรายการถูก/ผิดด้วย `loanRules.js` ไม่เขียนทับข้อมูลเดิม + เทสต์
- [x] T6.7 ตรวจว่าเทสต์เดิมของ `loanRules.js` และ `dateFormat.js` ยังผ่านโดยไม่ต้องแก้

- **เสร็จเมื่อ:** `npm test` ผ่านทั้งหมด

## เฟส 7: UI

- [x] T7.1 `LoginForm.jsx`: email + รหัสผ่าน, ปิดปุ่มระหว่างรอ, ข้อความผิดพลาดภาษาไทย, ไม่มีลิงก์สมัคร
- [x] T7.2 `App.jsx`: สลับหน้าเข้าสู่ระบบ/หน้าหลักตาม session, โหลด Loan จาก `loanRepo` หลังเข้าสู่ระบบ, ล้าง state เมื่อออกจากระบบ
- [x] T7.3 `AppHeader.jsx`: แสดง email, ThemeToggle, ปุ่มออกจากระบบ
- [x] T7.4 เชื่อมเพิ่ม/แก้ไข/คืนแล้ว/ยกเลิกการคืนกับ `loanRepo` อัปเดตหน้าจอจากแถวที่เซิร์ฟเวอร์คืนมา, ปิดปุ่มระหว่างบันทึก, บันทึกไม่สำเร็จให้คงข้อมูลในฟอร์ม
- [x] T7.5 สถานะ "กำลังโหลด…" และข้อความผิดพลาดเครือข่าย/session หมดอายุ (กลับหน้าเข้าสู่ระบบ)
- [x] T7.6 `LegacyImportBanner.jsx`: แสดงจำนวนข้อมูลเดิม, ปุ่มนำเข้า/ไม่นำเข้า, สรุปผลนำเข้า X ข้าม Y ตาม design.md ข้อ 9
- [x] T7.7 สไตล์หน้าเข้าสู่ระบบและแถบบน รองรับมือถือและโหมดมืด

- **เสร็จเมื่อ:** เข้าสู่ระบบแล้วใช้งานครบทุกอย่างในขอบเขตผ่านหน้าเว็บได้ ข้อมูลอยู่บน Supabase

## เฟส 8: ตรวจรับเวอร์ชัน 2

- [x] T8.1 รัน `npm test` ผ่านทั้งหมด
- [ ] T8.2 ทดลองในเบราว์เซอร์: เข้าสู่ระบบ → เพิ่ม → แก้ไข → กดคืน → ยกเลิกคืน → รีเฟรช (ยังเข้าสู่ระบบอยู่และข้อมูลครบ) → ออกจากระบบ — ชั้นข้อมูลผ่านแล้ว (Node + บัญชีทดสอบ) เหลือกดในหน้าเว็บ
- [x] T8.3 เปิดจากอีกเบราว์เซอร์/เครื่อง เข้าสู่ระบบแล้วเห็นข้อมูลเดียวกัน — ทดสอบด้วย client ที่สองของบัญชีทดสอบ เห็นข้อมูลชุดเดียวกัน
- [x] T8.4 ทดสอบ RLS: สร้างบัญชีทดสอบ B ชั่วคราวผ่าน Dashboard ยืนยันว่ามองไม่เห็น/แก้ไม่ได้ Loan ของเจ้าของ และ `anon` อ่าน/เขียนไม่ได้ (ถามก่อนลบบัญชีทดสอบ) — ทดสอบด้วยผู้ใช้จำลอง 2 คนใน SQL transaction แล้ว rollback (ไม่ต้องสร้าง/ลบบัญชีจริง)
- [ ] T8.5 ยืนยันว่า `DELETE` ถูกปฏิเสธ, signup ผ่าน API ใช้ไม่ได้, check constraint ปฏิเสธวันที่ผิดกติกา — DELETE ✓ check constraint ✓ เหลือ signup (รอปิดใน Dashboard)
- [ ] T8.6 ทดลองนำเข้าข้อมูลเดิม: มีข้อมูล, JSON เสีย, กดไม่นำเข้า, นำเข้าซ้ำไม่ได้ และข้อมูลเดิมใน localStorage ยังอยู่
- [ ] T8.7 ทดลองกรณีผิดพลาด: รหัสผ่านผิด, ปิดเครือข่ายระหว่างบันทึก — รหัสผ่านผิด ✓ ข้อความเครือข่ายล่ม ✓ (Node) เหลือลองในเบราว์เซอร์
- [x] T8.8 ตรวจว่าไม่มี key จริงใน git และไม่มีฟีเจอร์นอกขอบเขต (ลบ, สมัครสมาชิก, รีเซ็ตรหัสผ่าน ฯลฯ) ศัพท์ในหน้าเว็บตรงกับ CONTEXT.md