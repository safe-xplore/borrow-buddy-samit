# Borrow Buddy: เอกสารออกแบบ (เวอร์ชัน 2)

ศัพท์ทั้งหมดในเอกสารนี้อ้างอิงจาก [CONTEXT.md](./CONTEXT.md)

## 0. สิ่งที่เปลี่ยนจากเวอร์ชัน 1
- ย้ายที่เก็บ Loan จาก localStorage ไปตาราง `loans` บน Supabase (Postgres)
- เพิ่มการ **เข้าสู่ระบบ** ด้วย email + password สำหรับ **เจ้าของ** โดยปิดการสมัครสมาชิกเอง (บัญชีสร้างผ่าน Supabase Dashboard เท่านั้น)
- ใช้ Row Level Security (RLS) ให้ **บัญชีเจ้าของ** เห็นและแก้ได้เฉพาะ Loan ของตัวเอง
- เพิ่ม **การนำเข้าข้อมูลเดิม** จาก localStorage ครั้งเดียว
- กติกา Loan, สถานะ, การแสดงผล และการไม่มีการลบ Loan คงเดิม

## 1. ภาพรวมและเป้าหมาย
เว็บหน้าเดียวสำหรับ **เจ้าของ** ใช้บันทึกว่า **เพื่อน** ยืม **ของ** อะไร เมื่อไร ต้องคืนเมื่อไร และกดว่า **คืนแล้ว** ได้ ข้อมูลเก็บบนคลาวด์ เปิดจากเครื่องไหนก็ได้หลัง **เข้าสู่ระบบ**

## 2. ขอบเขต

**อยู่ในขอบเขต**
- เข้าสู่ระบบด้วย email + password และออกจากระบบ
- เพิ่ม Loan, แก้ไข Loan, ดูรายการ Loan (เฉพาะของบัญชีตัวเอง)
- กดคืนแล้ว (ระบุวันที่คืนจริง) และยกเลิกการคืน
- ค้นหา/กรองตามชื่อเพื่อน
- แสดงสถานะ และจำนวนวันเกินกำหนด
- นำเข้าข้อมูลเดิมจาก localStorage ครั้งเดียว

**นอกขอบเขต (เวอร์ชัน 2)**
- สมัครสมาชิกเอง, ลืมรหัสผ่าน/รีเซ็ตรหัสผ่านผ่านหน้าเว็บ, ล็อกอินด้วย Google/OAuth/Magic link
- ลบ Loan (ทั้งใน UI และในฐานข้อมูล), แจ้งเตือน, เลื่อนกำหนดคืน, รูปภาพของ
- ทะเบียนรายชื่อเพื่อน/ของแยก, การตรวจของซ้ำซ้อน
- ใช้งานออฟไลน์, ซิงก์แบบ realtime ระหว่างหลายแท็บ/เครื่อง
- แชร์ Loan ระหว่างบัญชี, สิทธิ์ผู้ดูแลในหน้าเว็บ

## 3. เทคโนโลยี
- React + Vite, ภาษา JavaScript (ไม่ใช้ TypeScript)
- Supabase: Postgres (เก็บ Loan), Auth (email + password), RLS
- ไลบรารี `@supabase/supabase-js` ฝั่งเบราว์เซอร์
- ค่าเชื่อมต่อใน `.env.local` (ไม่ commit): `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY` ใช้เฉพาะ publishable/anon key ห้ามใช้ service role key ฝั่งเว็บ
- ทดสอบด้วย Vitest
- ค่าโหมดมืดยังเก็บใน localStorage (เป็นค่าตั้งของเครื่อง ไม่ใช่ข้อมูล Loan)

## 4. โมเดลข้อมูล

**Loan ในแอป** (รูปแบบเดิม ใช้ในโค้ดฝั่ง UI และ `lib/`)
| ฟิลด์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | string (uuid) | ฐานข้อมูลสร้างให้ |
| `friendName` | string | ชื่อเล่นของเพื่อน พิมพ์เองทุกครั้ง ห้ามว่าง |
| `itemName` | string | ชื่อของ ห้ามว่าง |
| `borrowedDate` | string `YYYY-MM-DD` | วันที่ยืม |
| `dueDate` | string `YYYY-MM-DD` | กำหนดคืน บังคับกรอก |
| `returnedDate` | string `YYYY-MM-DD` หรือ `null` | วันที่คืนจริง ว่างจนกว่าจะกดคืน |

**ตาราง `public.loans`** (Supabase)
| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | `uuid` PK | `default gen_random_uuid()` |
| `owner_id` | `uuid` not null | FK → `auth.users(id)`, `default auth.uid()` |
| `friend_name` | `text` not null | check: `btrim(friend_name) <> ''` |
| `item_name` | `text` not null | check: `btrim(item_name) <> ''` |
| `borrowed_date` | `date` not null | |
| `due_date` | `date` not null | check: `due_date >= borrowed_date` |
| `returned_date` | `date` null | check: `returned_date is null or returned_date >= borrowed_date` |
| `created_at` | `timestamptz` not null | `default now()` |
| `updated_at` | `timestamptz` not null | `default now()` อัปเดตด้วย trigger |

- index บน `owner_id`
- แปลงชื่อฟิลด์ camelCase ↔ snake_case ในชั้น repository เท่านั้น (`lib/loanMapper.js`) UI ไม่รู้จักชื่อคอลัมน์
- วันที่เก็บเป็น ค.ศ. (`date`) และแสดงผลเป็น พ.ศ. เท่านั้น (เช่น 24 ก.ย. 2569) สถานะและจำนวนวันเกินกำหนดไม่เก็บ คำนวณจากวันที่ทุกครั้ง
- check constraint ในฐานข้อมูลเป็นด่านสุดท้าย การตรวจหลักยังทำใน `loanRules.js` เพื่อแสดงข้อความภาษาไทย

## 5. กติกา

**สถานะ** (คำนวณจากวันนี้)
- **คืนแล้ว**: `returnedDate` ไม่เป็น null
- **เกินกำหนด**: ยังไม่คืน และวันนี้ > `dueDate`
- **ยังไม่คืน**: ยังไม่คืน และวันนี้ ≤ `dueDate` (ถึงกำหนดวันนี้พอดียังไม่เกิน)

**จำนวนวันเกินกำหนด** = วันนี้ − `dueDate` (นับเป็นวัน ใช้กับ Loan ที่เกินกำหนดเท่านั้น)

**การตรวจวันที่** (ปฏิเสธการบันทึกเมื่อผิดกติกา พร้อมข้อความภาษาไทย)
- `dueDate` ต้องไม่ก่อน `borrowedDate`
- `returnedDate` ต้องไม่ก่อน `borrowedDate`
- ชื่อเพื่อนและชื่อของห้ามว่าง

**การกดคืนแล้ว**
- ค่าเริ่มต้นของวันที่คืนจริงคือวันนี้ แก้เป็นวันย้อนหลังได้
- ยกเลิกการคืนได้ (ตั้ง `returnedDate` กลับเป็น null)

**การลบ Loan**
- ไม่มีเหมือนเดิม ไม่มีปุ่มลบ ไม่มีฟังก์ชันลบในโค้ด และไม่มี RLS policy สำหรับ `DELETE` (ถูกปฏิเสธโดยอัตโนมัติ)

## 6. การยืนยันตัวตนและสิทธิ์

**การเข้าสู่ระบบ**
- ใช้ Supabase Auth แบบ email + password (`signInWithPassword`)
- ปิด "Allow new users to sign up" ใน Supabase Dashboard (Authentication → Sign In / Providers) หน้าเว็บไม่มีฟอร์มสมัคร
- **บัญชีเจ้าของ** สร้างโดยผู้ดูแลผ่าน Dashboard (Add user) และยืนยัน email ไว้แล้ว
- เก็บ session ด้วยค่าเริ่มต้นของ supabase-js (คงอยู่หลังรีเฟรช) ติดตาม `onAuthStateChange` เพื่อสลับหน้าจอ
- ปุ่มออกจากระบบ (`signOut`) แล้วล้าง state Loan ในหน้าออกทันที
- ข้อความผิดพลาดเป็นภาษาไทย และไม่บอกว่า email หรือรหัสผ่านอันไหนผิด (เช่น "อีเมลหรือรหัสผ่านไม่ถูกต้อง")

**RLS บนตาราง `loans`**
- `alter table public.loans enable row level security`
- policy ทั้งหมดใช้กับ role `authenticated` เท่านั้น (`anon` เข้าถึงไม่ได้)
  - `SELECT`: `using (owner_id = (select auth.uid()))`
  - `INSERT`: `with check (owner_id = (select auth.uid()))`
  - `UPDATE`: `using (owner_id = (select auth.uid())) with check (owner_id = (select auth.uid()))`
  - `DELETE`: ไม่มี policy
- ฝั่งเว็บไม่ส่ง `owner_id` เอง ให้ใช้ default `auth.uid()` และห้ามแก้ `owner_id` ภายหลัง
- หลังสร้างตารางให้ตรวจ Security Advisor ของ Supabase ไม่มีคำเตือนเรื่อง RLS

## 7. หน้าจอ

**หน้าเข้าสู่ระบบ** (แสดงเมื่อยังไม่มี session)
- ช่อง email, รหัสผ่าน, ปุ่ม "เข้าสู่ระบบ" ปิดปุ่มระหว่างรอผล
- แสดงข้อความผิดพลาดภาษาไทย ไม่มีลิงก์สมัครสมาชิก

**หน้าหลัก** (แสดงเมื่อเข้าสู่ระบบแล้ว)
- **แถบบน**: email ของบัญชีที่ใช้อยู่, ปุ่มโหมดมืด, ปุ่ม "ออกจากระบบ"
- **แถบนำเข้าข้อมูลเดิม**: แสดงเมื่อพบ Loan ใน localStorage ที่ยังไม่เคยนำเข้า (ดูข้อ 9)
- **ฟอร์ม Loan** ใช้ทั้งเพิ่มและแก้ไข: เพื่อน, ของ, วันที่ยืม (ค่าเริ่มต้นวันนี้), กำหนดคืน กดแก้ไขที่รายการแล้วข้อมูลขึ้นฟอร์ม มีปุ่มยกเลิกการแก้ไข
- **ช่องค้นหา/กรอง** ตามชื่อเพื่อน
- **รายการ Loan** แยกกลุ่มตามลำดับ **เกินกำหนด → ยังไม่คืน → คืนแล้ว** แต่ละกลุ่มเรียงตามกำหนดคืนใกล้สุดก่อน
- แต่ละรายการแสดง: ชื่อของ, ชื่อเพื่อน, วันที่ยืม, กำหนดคืน, สถานะ, จำนวนวันเกินกำหนด (เฉพาะเกินกำหนด), วันที่คืนจริง (เฉพาะคืนแล้ว)
- ปุ่มต่อรายการ: **คืนแล้ว** (เฉพาะที่ยังไม่คืน), **ยกเลิกการคืน** (เฉพาะที่คืนแล้ว), **แก้ไข**
- ไม่มีปุ่มลบ

**สถานะการโหลดและข้อผิดพลาด**
- ระหว่างโหลดรายการแสดง "กำลังโหลด…"
- ระหว่างบันทึกปิดปุ่มที่เกี่ยวข้อง กันกดซ้ำ
- บันทึกไม่สำเร็จ (เครือข่าย/ฐานข้อมูล) แสดงข้อความภาษาไทย และคงข้อมูลที่กรอกในฟอร์มไว้ ไม่อัปเดตรายการในหน้าจนกว่าเซิร์ฟเวอร์ยืนยัน
- session หมดอายุหรือถูกปฏิเสธสิทธิ์ ให้กลับไปหน้าเข้าสู่ระบบพร้อมข้อความ

**การแสดงผล**
- ภาษาไทยทั้งหมด, วันที่ พ.ศ.
- รองรับหน้าจอมือถือ (layout ยืดหยุ่น)
- โหมดมืด สลับได้ด้วยปุ่ม และจำค่าที่เลือกไว้ (ทั้งหน้าเข้าสู่ระบบและหน้าหลัก)
- ใช้สี/ข้อความแยกสถานะให้ชัด ไม่พึ่งสีอย่างเดียว

## 8. โครงสร้างโค้ด (ระดับโฟลเดอร์)

แยกตรรกะล้วนออกจาก UI และแยกการคุยกับ Supabase ไว้ชั้นเดียว

```
borrow-buddy/
├── CONTEXT.md
├── design.md
├── .env.example             # ชื่อตัวแปรเปล่า ไม่มีค่าจริง
├── package.json
├── index.html
├── supabase/
│   └── migrations/          # SQL สร้างตาราง loans, constraint, trigger, RLS
└── src/
    ├── main.jsx
    ├── App.jsx              # สลับหน้าเข้าสู่ระบบ/หน้าหลักตาม session
    ├── components/
    │   ├── LoginForm.jsx
    │   ├── AppHeader.jsx    # email, ThemeToggle, ออกจากระบบ
    │   ├── LegacyImportBanner.jsx
    │   ├── LoanForm.jsx
    │   ├── LoanList.jsx
    │   ├── LoanItem.jsx
    │   ├── SearchBox.jsx
    │   └── ThemeToggle.jsx
    └── lib/
        ├── loanRules.js     # สถานะ, จำนวนวันเกิน, ตรวจวันที่, เรียง/จัดกลุ่ม (เดิม)
        ├── dateFormat.js    # แปลง ISO ค.ศ. เป็นข้อความ พ.ศ. (เดิม)
        ├── loanMapper.js    # แปลง Loan ↔ แถวตาราง loans (ฟังก์ชันล้วน)
        ├── supabaseClient.js# สร้าง client จากตัวแปร env ครั้งเดียว
        ├── auth.js          # เข้าสู่ระบบ, ออกจากระบบ, ติดตาม session
        ├── loanRepo.js      # list / create / update Loan ผ่าน Supabase (ไม่มี delete)
        ├── authErrors.js    # แปลงข้อผิดพลาด Supabase เป็นข้อความไทย (ฟังก์ชันล้วน)
        └── legacyStorage.js # อ่าน Loan เดิมจาก localStorage + ธงนำเข้าแล้ว (ปรับจาก storage.js)
```

- ฟังก์ชันใน `lib/` ที่เป็นตรรกะล้วนรับ "วันนี้" เป็นพารามิเตอร์ ไม่เรียกเวลาจริงข้างใน
- `loanRepo.js` และ `auth.js` รับ client เป็นพารามิเตอร์ได้ เพื่อใช้ client ปลอมตอนทดสอบ

## 9. การเก็บข้อมูลและการนำเข้าข้อมูลเดิม

**การเก็บข้อมูล**
- Loan ทั้งหมดอยู่ในตาราง `loans` บน Supabase ไม่เก็บสำเนาใน localStorage
- โหลดรายการหลังเข้าสู่ระบบสำเร็จ และหลังบันทึกแต่ละครั้งใช้แถวที่เซิร์ฟเวอร์คืนมาอัปเดตรายการในหน้า
- การแก้ไขและกดคืนแล้ว/ยกเลิกการคืนใช้ `update` เฉพาะแถวที่ `id` ตรงกัน

**การนำเข้าข้อมูลเดิม (ครั้งเดียว)**
- หลังเข้าสู่ระบบ ถ้าคีย์ localStorage เดิมมี Loan และยังไม่มีธง "นำเข้าแล้ว" ให้แสดงแถบ "พบข้อมูลเดิม N รายการในเครื่องนี้" พร้อมปุ่ม "นำเข้า" และ "ไม่นำเข้า"
- นำเข้า: ตรวจทุกรายการด้วย `loanRules.js` ก่อน ส่งเฉพาะรายการที่ถูกต้องแบบ insert ครั้งเดียว (ไม่ส่ง `id` เดิม ให้ฐานข้อมูลสร้างใหม่) แล้วสรุปผล "นำเข้า X รายการ ข้าม Y รายการ"
- สำเร็จแล้วตั้งธง "นำเข้าแล้ว" ใน localStorage ห้ามลบข้อมูลเดิมในคีย์เดิม (เก็บไว้เป็นสำรอง)
- ไม่นำเข้า: ตั้งธงเดียวกัน แถบจะไม่แสดงอีก
- ถ้า JSON เดิมเสีย ให้แจ้งเป็นภาษาไทยและไม่แสดงปุ่มนำเข้า ห้ามเขียนทับข้อมูลเดิม
- ถ้านำเข้าล้มเหลว ไม่ตั้งธง ให้ลองใหม่ได้

## 10. การทดสอบ

**Vitest (ตรรกะใน `lib/`)**
- เดิม: สถานะ, ขอบวันนี้ = กำหนดคืน, จำนวนวันเกินกำหนด, การตรวจวันที่, การจัดกลุ่มและเรียงลำดับ, การแปลงเป็น พ.ศ., การกรองตามชื่อเพื่อน
- `loanMapper.js`: แปลงไป-กลับครบทุกฟิลด์, `returnedDate` null, ไม่ส่ง `id`/`owner_id` ตอน insert
- `authErrors.js`: ข้อผิดพลาดที่รู้จักเป็นข้อความไทย, ไม่รู้จักเป็นข้อความทั่วไป
- `legacyStorage.js`: ไม่มีข้อมูล, JSON เสีย, มีธงนำเข้าแล้ว, แยกรายการถูก/ผิด
- `loanRepo.js` ด้วย client ปลอม: เรียกตาราง/คอลัมน์ถูกต้อง, ส่งต่อข้อผิดพลาด, ไม่มีฟังก์ชัน delete

**ตรวจฝั่ง Supabase (ด้วยมือหรือ SQL)**
- บัญชี A มองไม่เห็น/แก้ไม่ได้ Loan ของบัญชี B
- ผู้ไม่ได้เข้าสู่ระบบ (`anon`) อ่าน/เขียนไม่ได้
- `DELETE` ถูกปฏิเสธทุกกรณี
- check constraint ปฏิเสธวันที่ผิดกติกาและชื่อว่าง
- สมัครสมาชิกผ่าน API ไม่ได้ (signup ถูกปิด)
- Security Advisor ไม่มีคำเตือน

## 11. ประเด็นที่ตั้งใจไม่ทำ
สมัครสมาชิกเอง, รีเซ็ตรหัสผ่านผ่านหน้าเว็บ, ลบ Loan, แจ้งเตือน, เลื่อนกำหนด, รูปภาพ, ทะเบียนเพื่อน/ของ, ออฟไลน์, realtime, แชร์ข้อมูลระหว่างบัญชี หากต้องการในอนาคตให้ทบทวน CONTEXT.md ก่อน
